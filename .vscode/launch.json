{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Debugging with Clang",
            "type": "lldb",  // 使用 lldb 调试器
            "request": "launch",
            "preLaunchTask": "clang build",  // 先执行 "clang build" 任务
            "program": "${workspaceFolder}/build/cpp",  // 调试已构建的可执行文件
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
